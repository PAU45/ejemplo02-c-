<Window x:Class="WpfApp1.Conductores"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Listado de Conductores" Height="350" Width="500">
    <Grid Margin="10">
        <StackPanel>
            <!-- Tabla con la lista -->
            <DataGrid x:Name="dgConductores" AutoGenerateColumns="False" Height="200" Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                    <DataGridTextColumn Header="Licencia" Binding="{Binding Licencia}" Width="*"/>
                    <DataGridTextColumn Header="Transporte" Binding="{Binding Transporte}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Botones de acciones -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Agregar" Width="80" Margin="5" Click="Agregar_Click"/>
                <Button Content="Editar" Width="80" Margin="5" Click="Editar_Click"/>
                <Button Content="Eliminar" Width="80" Margin="5" Click="Eliminar_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
